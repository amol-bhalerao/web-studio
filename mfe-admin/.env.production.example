# Copy to .env.production before `npm run build` (optional).

# If the API is not same-origin /api/v1:
# VITE_API_BASE=https://www.example.com/api/v1

# Base path is normally set by CLI when building for /admin/:
#   npm run build -- --base /admin/
